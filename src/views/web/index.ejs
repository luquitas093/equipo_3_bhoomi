<!DOCTYPE html>
<html lang="en">

    <%- include("../partials/head.ejs")%>

<body>

    <%- include("../partials/header.ejs")%>

    <main class="main-content">

        <%- include("../partials/menuMediaQuery.ejs")%>

        <!-- BANNER (FUTURO CARROUSEL)-->
        <div class="main-products">
            <figure class="banner-carrousel">
                <img src="img/banner-galeria.gif" alt="banner-principal">
            </figure>

            <!--CATEGORIAS DE PRODUCTOS-->
            <section>
                <ul class="categories">
                    <% for (let i = 0; i < 1; i++) { %> 
                    <li>
                        <a href="/productos/<%=categories[0].id%>"><%=categories[0].name%></a>
                    </li>
                    <li>
                        <a href="/productos/<%=categories[1].id%>"><%=categories[1].name%></a>
                    </li>
                    <li class="categories-hidden">
                        <a href="/productos/<%=categories[2].id%>"><%=categories[2].name%></a>
                    </li>
                    <li>
                        <a href="/productos/<%=categories[3].id%>"><%=categories[3].name%></a>
                    </li>
                    <li class="categories-hidden">
                        <a href="/productos/<%=categories[4].id%>"><%=categories[4].name%></a>
                    </li>
                    <li class="categories-hidden">
                        <a href="/productos/<%=categories[5].id%>"><%=categories[5].name%></a>
                    </li>
                    <% } %>
                </ul>
            </section>

            <!--PRODUCTOS DESTACADOS-->
            <section class="best-products">
                <% for(let i= 0; i < products.length; i++){ %>
                <article class="product-article">
                    <a href="/productos/detalleproducto/<%= products[i].id%>"> <img src="img/products/<%= products[i].image%>" alt=<%= products[i].name%>></a>
                    <div class="product-description">
                        <a href="/productos/detalleproducto/<%= products[i].id%>" class="product-name"><%= products[i].name%></a>
                        <p class="product-price">$<%= products[i].price%></p>
                    </div>
                </article>
                <% } %>
                <div class="all-products">
                    <a href="/productos">VER TODOS LOS PRODUCTOS</a>
                </div>
            </section>

            <!-- BANNER-BOTTOM (FUTURO CARROUSEL)-->
            <figure class="banner-carrousel-bottom">
                <img src="img/banner-bottom.gif" alt="banner-bottom">
            </figure>

    </main>

    <%- include("../partials/footer.ejs")%>

</body>

</html>